import { AmpVisibleDirective } from './amp-visible.directive';

describe('AmpVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new AmpVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
